import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const TabOneScreen = () => {
  return (
    <SafeAreaView>
      <Text className="text-red-500">TabOneScreen</Text>
    </SafeAreaView>
  );
};

export default TabOneScreen;
